# Sketchpad

A React + Vite sketchpad built on Paper.js.

## Development

Install dependencies:

```bash
npm install
```

Start the local dev server:

```bash
npm run dev
```

## Checks

Run TypeScript:

```bash
npm run check
```

Build the production bundle:

```bash
npm run build
```
